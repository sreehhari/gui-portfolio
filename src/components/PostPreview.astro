---
const { post } = Astro.props;

const dateFormat = new Intl.DateTimeFormat("es-MX", {
  day: "numeric",
  month: "short",
  year: "numeric",
});

function getFormattedDate(date: string | number | Date) {
  return dateFormat.format(new Date(date));
}

const date = new Date(post.data.publishDate);
const datetime = date.toISOString();
const postDate = getFormattedDate(date);
---

<div class="flex flex-row gap-2 items-center text-sm">
  <time datetime={datetime} class="min-w-[120px]">{postDate}</time>
  <a class="hover:underline hover:text-cyan-700" href={`/blog/${post.slug}/`}>
    {post.data.title}
  </a>
</div>
